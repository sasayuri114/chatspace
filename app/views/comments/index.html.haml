%body
  .flash-box
  .wrapper
    .chat-side
      = render "left-header"
      = render "left-main"
    
    .chat-main
      = render "right-header"
      = render "right-main"
      = render "right-footer"
   